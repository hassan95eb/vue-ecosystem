// Pulls in the custom matcher declarations registered by the shared test setup
// (`@vue-ecosystem/core/test-setup`, wired in from the root vitest config).
// Every package with tests needs this one-liner -- see CONTRIBUTING.md.
import '@vue-ecosystem/core/matchers'
