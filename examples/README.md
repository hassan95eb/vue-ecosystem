# Examples

Standalone, copy-pasteable usage examples — one directory per scenario.

Right now the live demo lives in [`apps/playground`](../apps/playground); this folder
fills up as packages reach their MVP. An example here should be runnable on its own,
without the monorepo.
