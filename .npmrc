# Keep transitive deps out of the root of node_modules so a package can never
# accidentally import something it did not declare.
strict-peer-dependencies=false
auto-install-peers=true
provenance=true
